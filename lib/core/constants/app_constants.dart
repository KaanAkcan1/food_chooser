class AppConstants {
  // Base URL for the API
  static const String baseUrl = "https://api.example.com/";

  // API Endpoints
  static const String getIngredients = "${baseUrl}ingredients";
  static const String getRecipes = "${baseUrl}recipes";
  static const String addRecipe = "${baseUrl}addRecipe";
  static const String addIngredient = "${baseUrl}addIngredient";
}
